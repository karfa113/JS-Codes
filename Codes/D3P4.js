// Maths and numbers

const price = 100
console.log(price.toFixed(2)) // use toFixed function for precision values after decimal

const hundredes = 100000000
console.log(hundredes.toLocaleString('en-IN')); // use to add koma after every 2 digits


