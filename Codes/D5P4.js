// forof loop 
let arr = [1, 2, 3, 4, 5];
for (let num of arr) {
  //console.log(num);
}

//forin loop
myobj = {
    js: "JavaScript",
    cpp: "C++",
    py: "Python"
};

for (let key in myobj) {
    console.log(key + ": " + myobj[key]); // way to print object properties
}