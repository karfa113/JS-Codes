// Comparison in JS

console.log("2" === 2); // use === to check value as well as data type


// Premetive data types:
// 7 types: string, number, boolean, null, symbol, bigint, undefined

// Reference (Non-Premetive data types):
// Arrays, objects and functions